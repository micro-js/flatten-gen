{
  "name": "@f/flatten-gen",
  "description": "Flatten a generator.",
  "repository": "git://github.com/micro-js/flatten-gen.git",
  "version": "1.1.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/is-generator": "^1.1.1",
    "@f/slice": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
