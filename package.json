{
  "name": "@f/flatten-gen",
  "description": "Flatten a generator.",
  "repository": "git://github.com/micro-js/flatten-gen.git",
  "version": "1.2.3",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "make build",
    "postpublish": "make clean",
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/is-generator": "^1.3.2",
    "@f/is-generator-object": "^1.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.4.0",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.3.13",
    "babel-polyfill": "^6.3.13",
    "tape": "^4.2.2"
  }
}
