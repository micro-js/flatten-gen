{
  "name": "@micro-js/flatten-gen",
  "description": "Flatten a generator.",
  "repository": "git://github.com/micro-js/flatten-gen.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@micro-js/is-generator": "^1.1.1",
    "@micro-js/is-generator-object": "^1.1.0",
    "@micro-js/slice": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
